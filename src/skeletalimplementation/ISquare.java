package skeletalimplementation;

/**
 * Το interface ISquare κάνει extends ένα
 * generic interface, το IRectangle και
 * προσθέτει επιπλέον μεθόδους
 */
public interface ISquare extends IRectangle{

    double getDiagonal();
}
